name: Cron Job

on:
  schedule:
    - cron: '* * * * *'

jobs:
  build:
    name: Do something on schedule
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify Build
        run: curl -X POST -d {} NETLIFY_BUILD_HOOK_URL